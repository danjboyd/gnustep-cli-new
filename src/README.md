# Source Layout

This directory is reserved for project source code.

Planned high-level areas:

- bootstrap-related shared helpers, if any are introduced later
- the full Objective-C/GNUstep CLI application
- shared data-model or generation helpers that belong in the repository proper

The bootstrap entry points currently live in `scripts/bootstrap/` because they are intended to be directly invokable scripts.

